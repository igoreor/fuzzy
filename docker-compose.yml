version: '3.8'

services:
  fuzzy-app:
    build: .
    container_name: fuzzy-apresentacoes
    ports:
      - "8501:8501"
    volumes:
      # Mapeia o código local para desenvolvimento hot-reload (opcional)
      - .:/app
    environment:
      - STREAMLIT_SERVER_PORT=8501
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
      - STREAMLIT_SERVER_HEADLESS=true
      - STREAMLIT_BROWSER_GATHER_USAGE_STATS=false
    restart: unless-stopped
